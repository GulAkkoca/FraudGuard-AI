from models.product_schema import Product


def extract_product_from_raw_text(raw_text: str, source_url: str) -> Product | None:
    # Gemini entegrasyonu burada yapilacak. Eksik alanlar icin tahmin yapmak yerine null donulmeli.
    return None

